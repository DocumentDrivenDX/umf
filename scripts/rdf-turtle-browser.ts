import {chromium} from 'playwright';
const cases=(await Bun.file('fixtures/rdf/turtle/results.json').json()).results;for(const c of cases){c.raw=await Bun.file(c.path).text();if(c.outputPath)c.nquads=await Bun.file(c.outputPath).text();for(const e of c.exports??[])e.output=await Bun.file(e.path).text();}
const built=await Bun.build({entrypoints:['src/index.ts'],outdir:'.cache/rdf-turtle-browser',naming:'umf.js',target:'browser',format:'esm'});if(!built.success)throw Error(built.logs.join('\n'));
const server=Bun.serve({hostname:'127.0.0.1',port:0,fetch(req){return new URL(req.url).pathname==='/umf.js'?new Response(Bun.file('.cache/rdf-turtle-browser/umf.js'),{headers:{'content-type':'text/javascript'}}):new Response('<!doctype html><title>RDF Turtle</title>');}});let browser;
try{browser=await chromium.launch({headless:true,...(process.env.UMF_CHROMIUM_PATH?{executablePath:process.env.UMF_CHROMIUM_PATH}:{})});const page=await browser.newPage();await page.goto('http://127.0.0.1:'+server.port);const result=await page.evaluate(async(cases)=>{const path='/umf.js',u=await import(path);let accepted=0,rejected=0,formats=0,edits=0;for(const c of cases){let d;try{d=u.importRdfTurtle(c.raw,{id:c.id,baseIRI:c.baseIRI});}catch(e){if(c.positive)throw e;rejected++;continue;}if(!c.positive)throw Error('Accepted negative');accepted++;if(JSON.stringify(u.getRdfQuads(d))!==JSON.stringify(c.terms)||u.exportRdfNQuads(d)!==c.nquads)throw Error('Graph differs');for(const format of ['json','yaml']){const restored=u.readDocument(u.writeDocument(d,format),format);if(u.exportRdfTurtle(restored)!==c.raw)throw Error('Source differs');formats++;if(c.editIndex>=0){const q=u.getRdfQuads(d)[c.editIndex],candidate=u.proposeRdfQuadEdit(restored,c.editIndex,{...q,object:{kind:'literal',value:'UMF reviewed literal',datatype:'http://www.w3.org/2001/XMLSchema#string'}}).document;if(u.exportRdfTurtle(u.readDocument(u.writeDocument(candidate,format),format))!==c.exports.find((e:any)=>e.format===format).output)throw Error('Edit differs');edits++;}}}
 const named=u.importRdfNQuads('<urn:s> <urn:p> <urn:o> <urn:g> .',{id:'named'});let blocked=false;try{u.exportRdfTurtle(named);}catch{blocked=true;}if(!blocked)throw Error('Named graph discarded');return {accepted,rejected,formats,edits,namedGraphLossBlocked:blocked,nodeGlobalsAbsent:!('process' in globalThis)&&!('Buffer' in globalThis)};},cases);if(result.accepted!==219||result.rejected!==94||result.formats!==438||result.edits!==204||!result.nodeGlobalsAbsent)throw Error('Browser baseline changed');await Bun.write('fixtures/rdf/turtle/browser-results.json',JSON.stringify({...result,browser:browser.version()},null,2)+'\n');console.log(result);}finally{await browser?.close();server.stop(true);}
