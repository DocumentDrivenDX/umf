export {};
const physical=await Bun.file('spec/extensions/parquet/physical.schema.json').json(),value=physical.properties.pages.items.properties.values.items,ref={$ref:'#/$defs/rowValue'},obj=(properties:any)=>({type:'object',required:Object.keys(properties),properties,additionalProperties:false});
const s={$schema:physical.$schema,$id:'urn:umf:parquet:rows:0.1.0',type:'object',required:['source','status','complete','diagnostics'],properties:{source:physical.properties.source,status:{enum:['assembled','blocked']},complete:{const:false},diagnostics:physical.properties.diagnostics,rows:{type:'array',maxItems:10000,items:ref}},additionalProperties:false,$defs:{rowValue:{oneOf:[{type:'null'},obj({kind:{const:'physical'},value}),obj({kind:{const:'record'},fields:{type:'array',items:obj({index:{type:'integer',minimum:1},name:{type:'string'},value:ref})}}),obj({kind:{const:'repeated'},items:{type:'array',maxItems:100000,items:ref}})]}}};await Bun.write('spec/extensions/parquet/rows.schema.json',JSON.stringify(s,null,2)+'\n');
