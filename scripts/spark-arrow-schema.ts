export {};
const string={type:'string'},boolean={type:'boolean'},object=(properties:Record<string,unknown>,required=Object.keys(properties))=>({type:'object',properties,required,additionalProperties:false});
const schema={$schema:'https://json-schema.org/draft/2020-12/schema',$id:'urn:umf:projection:spark-arrow:0.1.0',...object({status:{enum:['blocked','projected']},complete:{const:false},source:{$ref:'urn:umf:core:0.1.0'},policy:object({id:{type:'string',minLength:1},timestampUtc:boolean,largeTypes:boolean,rejectNestedDuplicates:boolean,lossPolicy:{enum:['strict','allow-reported-loss']}}),issues:{type:'array',items:object({path:string,code:string,classification:{enum:['loss','unsupported','representation-change']},detail:string,retainedInSource:{const:true}})},target:{$ref:'urn:umf:core:0.1.0'}},['status','complete','source','policy','issues']),allOf:[{if:{properties:{status:{const:'projected'}}},then:{properties:{target:{}},required:['target']},else:{not:{properties:{target:{}},required:['target']}}}]};
await Bun.write('spec/projections/spark-arrow.schema.json',JSON.stringify(schema,null,2)+'\n');
