import {chromium} from 'playwright';
const base='fixtures/parquet/logical/',report=await Bun.file(base+'results.json').json(),cases=[];for(const c of report.results)cases.push({...c,bytes:Array.from(new Uint8Array(await Bun.file(c.path).arrayBuffer()))});
const built=await Bun.build({entrypoints:['src/index.ts'],outdir:'.cache/parquet-logical-browser',naming:'umf.js',target:'browser',format:'esm'});if(!built.success)throw Error(built.logs.join('\n'));
const server=Bun.serve({hostname:'127.0.0.1',port:0,fetch(req){return new URL(req.url).pathname==='/umf.js'?new Response(Bun.file('.cache/parquet-logical-browser/umf.js'),{headers:{'content-type':'text/javascript'}}):new Response('<!doctype html><title>Parquet logical annotations</title>');}});let browser;
try{browser=await chromium.launch({headless:true,...(process.env.UMF_CHROMIUM_PATH?{executablePath:process.env.UMF_CHROMIUM_PATH}:{})});const page=await browser.newPage();await page.goto('http://127.0.0.1:'+server.port);const result=await page.evaluate(async(cases)=>{const path='/umf.js',u=await import(path);let checked=0,blocked=0;for(const c of cases){const r=u.inspectParquetLogicalTypes(u.captureParquet(Uint8Array.from(c.bytes),{id:c.id})),diagnostics=r.diagnostics.filter((d:any)=>d.code.startsWith('PARQUET_LOGICAL')||d.code.startsWith('PARQUET_LEGACY')||d.code==='PARQUET_DECIMAL_SMALL_INT64');if(r.status!==c.status||JSON.stringify(r.annotations)!==JSON.stringify(c.annotations)||JSON.stringify(diagnostics)!==JSON.stringify(c.diagnostics))throw Error('Logical inspection differs: '+c.id);if(JSON.stringify(Array.from(u.exportParquetCapture(r.source)))!==JSON.stringify(c.bytes))throw Error('Source changed');if(r.status==='checked')checked++;else blocked++;}return {checked,blocked,nodeGlobalsAbsent:!('process' in globalThis)&&!('Buffer' in globalThis)};},cases);if(result.checked!==54||result.blocked!==15||!result.nodeGlobalsAbsent)throw Error('Browser baseline changed');await Bun.write(base+'browser-results.json',JSON.stringify({...result,browser:browser.version()},null,2)+'\n');console.log(result);}finally{await browser?.close();server.stop(true);}
