export {};
const graph=await Bun.file('spec/extensions/dbt-manifest/graph-schema.json').json(),str={type:'string'},node={...graph.properties.nodes.items,properties:{...graph.properties.nodes.items.properties,depth:{type:'integer',minimum:0,maximum:128}},required:[...graph.properties.nodes.items.required,'depth']};
const object=(properties:any)=>({type:'object',properties,required:Object.keys(properties),additionalProperties:false});
const selection=object({status:{enum:['selected','blocked']},complete:{const:false},roots:{type:'array',items:str,minItems:1,maxItems:64,uniqueItems:true},includeMacros:{type:'boolean'},maxDepth:{type:'integer',minimum:0,maximum:128},maxNodes:{type:'integer',minimum:1,maximum:10000},nodes:{type:'array',items:node},edges:graph.properties.edges,boundary:{type:'array',items:object({edge:graph.properties.edges.items,reason:{enum:['macro-excluded','depth-limit','node-limit']}})},diagnostics:graph.properties.diagnostics});
await Bun.write('spec/extensions/dbt-manifest/selection-schema.json',JSON.stringify({$schema:graph.$schema,$id:'urn:umf:dbt:dependency-selection:0.1.0',...object({source:{$ref:'urn:umf:core:0.1.0'},selection})},null,2)+'\n');
