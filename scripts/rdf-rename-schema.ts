export {};
const str={type:'string'},obj=(properties:any,required=Object.keys(properties))=>({type:'object',properties,required,additionalProperties:false});
const schema=obj({source:{$ref:'urn:umf:core:0.1.0'},from:{type:'string',minLength:1,maxLength:4096},to:{type:'string',minLength:1,maxLength:4096},status:{enum:['candidate','blocked']},complete:{const:false},changes:{type:'array',items:obj({path:str,before:str,after:str})},candidate:{$ref:'urn:umf:core:0.1.0'},diagnostics:{type:'array',items:obj({code:str,path:str,message:str,severity:{enum:['error','warning']}})}},['source','from','to','status','complete','changes','diagnostics']);
await Bun.write('spec/extensions/rdf/rename-schema.json',JSON.stringify({$schema:'https://json-schema.org/draft/2020-12/schema',$id:'urn:umf:rdf:rename:0.1.0',...schema,allOf:[{if:{properties:{status:{const:'candidate'}}},then:{required:['candidate']},else:{not:{required:['candidate']},properties:{changes:{maxItems:0}}}}]},null,2)+'\n');
